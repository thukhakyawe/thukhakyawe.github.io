# Thu Kha Kyawe — GitHub Pages

Personal SRE / Cloud Engineer portfolio.

## Deploy

Put `index.html` and `styles.css` in the root of the `thukhakyawe.github.io` repository and enable GitHub Pages from the `main` branch.

## Before publishing

Replace `your-email@example.com` in `index.html` with the preferred contact email.

The project cards are intentionally based only on the technologies and hands-on work described in the source profile. Replace them with concrete repository/project links as appropriate.
